const {Router} = require('express');

const UserController = {
    get router(){
        const router = Router();
        //TODO
        // Populate with route handlers
        return router;
    }
}

module.exports = UserController;